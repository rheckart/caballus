# Build output, not source.
.output/
.nitro/
.tanstack/
dist/

# Generated by the router; regenerated on every build (eslint.config.ts says
# the same about lint).
src/routeTree.gen.ts

# Whiteboard photographs are not source either, and the directory may not
# exist on every machine.
fhhr-whiteboard/

# The design canvas artboards are generated by `design/build.py`, and the
# canvas manifest with them. Formatting them here would put the working tree
# permanently at odds with the generator's own output.
design/
